# criando tupla de números
numbers = (1, 2, 3, 4, 5)

# acessando os elementos da tupla
second_number = numbers[1]
print(second_number)

# tentando modificar a tupla (vai dar em erro)
numbers[1] = 6
print(numbers)

# criando tupla de strings
words = ('hello', 'world', 'goodbye')

# acessando os elementos da tupla
second_word = words[1]
print(second_word)