git init
echo "Hello World" > nome_arquivo.txt // essa linha é para alterar dentro do arquivo
git add .
git commit -m "Comentário"
git branch -M main
git remote add origin https://github.com/shinzatokazuya/nome_do_codespace.git
git push -u origin main

-- Atualizar do meu pc pro git
git pull origin main
-- Se der conflito usar --allow-unrelated-histories
-- Fica assim
git pull origin main --allow-unrelated-histories
git add .
git commit -m "Update"
git push origin main

-- Atualizar do git pro meu pc
git status
git add .
git commit -m "Update"
git push origin main
